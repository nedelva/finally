---
phase: 05-one-command-delivery
plan: 05
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - test/e2e/02-watchlist.spec.ts
  - test/e2e/03-trading.spec.ts
  - test/e2e/04-portfolio-viz.spec.ts
  - test/e2e/05-chat.spec.ts
  - test/e2e/06-sse-reconnect.spec.ts
autonomous: true
requirements: [OPS-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All six PLAN.md section 12 scenarios are covered by specs that run against the packaged container: fresh start, watchlist add/remove, buy, sell, portfolio visuals, mocked chat with inline trade execution, and SSE reconnect"
    - "`docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with all six spec files present"
    - "Every spec except `01-fresh-start` creates whatever state it asserts on, so no spec depends on an earlier spec's side effects beyond the seeded defaults"
    - "The SSE-reconnect spec targets the connection dot through `page.getByRole('status')` and asserts on its `data-status` attribute — never on a Tailwind colour class and never on a bare `[data-status]` selector that would also match the chat action pills"
    - "The chat spec exercises the deterministic `LLM_MOCK` path only, covering a successful trade action, a watchlist action, and a validation failure reported inline"
    - "A rejected trade (selling more shares than held) leaves cash and positions unchanged and surfaces a visible error"
  prohibitions:
    - "MUST NOT invent a `data-testid` that has not been verified to exist in the component source — a spec asserting on a fabricated selector fails for the wrong reason and teaches nothing"
    - "MUST NOT use a fixed `page.waitForTimeout` sleep in place of a retrying assertion; a sleep long enough to pass on this machine is a latent flake on a slower one"
  artifacts:
    - path: "test/e2e/02-watchlist.spec.ts"
      provides: "Add a ticker, remove a ticker, reject a malformed ticker"
    - path: "test/e2e/03-trading.spec.ts"
      provides: "Buy, sell, and a refused over-sell with cash and positions unchanged"
    - path: "test/e2e/04-portfolio-viz.spec.ts"
      provides: "Heatmap tile, positions table row, and a plotted P&L chart point"
    - path: "test/e2e/05-chat.spec.ts"
      provides: "Mocked chat: grounded reply, inline executed trade pill, inline failed pill"
    - path: "test/e2e/06-sse-reconnect.spec.ts"
      provides: "Connection dot goes non-connected offline and returns to connected on reconnect"
  key_links:
    - from: "test/e2e/06-sse-reconnect.spec.ts"
      to: "frontend/components/ConnectionDot.tsx"
      via: "selects the dot by its unique role=status and asserts the data-status attribute it already exposes as a stable contract"
      pattern: "getByRole\\(['\"]status['\"]\\)"
    - from: "test/e2e/05-chat.spec.ts"
      to: "backend/app/llm/mock.py"
      via: "phrases messages to match the mock's buy/sell and add/remove regexes so every dispatch path is reachable"
      pattern: "chat-action-pill"
    - from: "test/e2e/03-trading.spec.ts"
      to: "frontend/components/TradeBar.tsx"
      via: "drives the trade bar through its existing testids and asserts on its confirmation and error slots"
      pattern: "trade-bar-"
---

<objective>
Complete the PLAN.md section 12 E2E matrix so the packaged container is validated across every user
journey the product promises, not just the first screen.

Purpose: plan 05-04 proved the harness runs and fails honestly with one scenario. This plan makes
the suite actually cover the product — watchlist edits, buying, selling, refused trades, the
portfolio visuals, the AI copilot under mock, and SSE reconnection — so a regression anywhere in
Phases 1-4 is caught against the shipped artifact.

Output: five spec files under `test/e2e/`, and a full suite that exits 0.

**Two inherited rules that are not negotiable.** (1) The suite command is exactly
`docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`
— copy it verbatim from plan 05-04; do not re-derive it. (2) The two-digit filename prefix is the
run-order mechanism: with `workers: 1` and `fullyParallel: false`, Playwright runs spec files in
sorted order, `01-fresh-start` must run before anything that trades, and renaming a spec silently
reorders the suite.

**The selector contract is fixed and verified.** Every `data-testid` named below was grepped out of
the component source. Do not invent selectors, and do not assert on Tailwind class strings — this
codebase deliberately exposes `data-testid` and `data-status` as stable contracts, and its colour
classes are arbitrary-value forms like `bg-[var(--color-up)]` that will never match a `/green/`
regex.
</objective>

<execution_context>
@/Users/valeriu/AICourses/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/valeriu/AICourses/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-one-command-delivery/05-RESEARCH.md
@.planning/phases/05-one-command-delivery/05-PATTERNS.md
@test/playwright.config.ts
@test/docker-compose.test.yml
@frontend/components/Watchlist.tsx
@frontend/components/WatchlistRow.tsx
@frontend/components/TradeBar.tsx
@frontend/components/PositionsTable.tsx
@frontend/components/Heatmap.tsx
@frontend/components/ConnectionDot.tsx
@frontend/components/chat/ChatPanel.tsx
@backend/app/llm/mock.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watchlist and trading journeys against the container</name>

  <precondition>Plan 05-04's harness runs green — `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with only `01-fresh-start.spec.ts` present.</precondition>

  <files>test/e2e/02-watchlist.spec.ts, test/e2e/03-trading.spec.ts</files>

  <read_first>
    - `frontend/components/Watchlist.tsx` lines 94-146 — the verified testids `watchlist-add-form`, `watchlist-add-input`, `watchlist-add-submit`, `watchlist-add-error`, `watchlist-scroll-container`, `watchlist-loading`, `watchlist-load-error`, `watchlist-empty`
    - `frontend/components/WatchlistRow.tsx` lines 83-111 — `row-{ticker}`, `price-{ticker}`, `change-{ticker}`, `sparkline-{ticker}`, `remove-{ticker}`
    - `frontend/components/TradeBar.tsx` lines 90-144 — `trade-bar-ticker`, `trade-bar-quantity`, `trade-bar-buy`, `trade-bar-sell`, `trade-bar-hint`, `trade-bar-error`, `trade-bar-confirmation`
    - `frontend/components/PositionsTable.tsx` lines 66-111 — `positions-scroll-container`, `positions-loading`, `positions-load-error`, `positions-empty`, `position-row-{ticker}`
    - `frontend/components/Header.tsx` — `header-cash`, `header-total-value`
    - `frontend/lib/format.ts` — `formatMoney` renders currency as `$10,000.00` via an explicit `en-US` locale, so assertions are locale-independent
    - `test/e2e/01-fresh-start.spec.ts` — the spec style established in plan 05-04; match it
    - `.planning/phases/05-one-command-delivery/05-PATTERNS.md` — the verified-selector table in the `test/e2e/*.spec.ts` Pattern Assignment
  </read_first>

  <action>
Author `test/e2e/02-watchlist.spec.ts` covering three cases against the running container. First,
adding a ticker: fill `watchlist-add-input` with `PYPL`, click `watchlist-add-submit`, then assert
with a retrying expectation that `row-PYPL` becomes visible and that `price-PYPL` eventually shows a
numeric price rather than the em-dash placeholder — this proves the watchlist mutation propagated
to the running market data source, not just to the database. Second, removing it: click
`remove-PYPL` and assert `row-PYPL` detaches. Third, rejection: submit a malformed entry (an empty
submission, and a string that is not 1-5 alphanumeric characters) and assert `watchlist-add-error`
becomes visible while the row count in `watchlist-scroll-container` is unchanged.

Author `test/e2e/03-trading.spec.ts` covering buy, sell, and refusal. Buy: fill `trade-bar-ticker`
with `MSFT` and `trade-bar-quantity` with `2`, click `trade-bar-buy`, then assert
`trade-bar-confirmation` appears, `position-row-MSFT` appears in the positions table, and
`header-cash` has decreased from the value captured immediately before the click. Sell: with the
position held, sell `1` of `MSFT` and assert a confirmation, a still-present `position-row-MSFT`,
and an increased `header-cash`. Refusal: attempt to sell `9999` of `MSFT`, then assert
`trade-bar-error` becomes visible, `header-cash` is unchanged from its pre-attempt value, and
`position-row-MSFT` still reports the same quantity.

Both specs must be self-sufficient: `03-trading` buys `MSFT` itself rather than relying on any
earlier spec. Use `MSFT` here and leave `AAPL` to the chat spec so the two never contend over the
same position.

Use Playwright's retrying assertions throughout — `expect(locator).toBeVisible()`,
`expect(locator).toHaveText()`, `expect(locator).not.toBeAttached()` — and never a fixed
`page.waitForTimeout`. Prices tick roughly every 500ms and trades fill instantly, so the default
expect timeout is ample; where a longer bound is genuinely needed, pass an explicit `{ timeout }`
rather than sleeping.
  </action>

  <verify>
    <automated>npm --prefix test ci >/dev/null && npm --prefix test exec -- playwright test --list 2>&1 | grep -E '02-watchlist|03-trading'</automated>
    <fails_when>`playwright test --list` prints neither spec name (a TypeScript parse error or a `testDir` mismatch keeps the files from being collected), or the command exits non-zero</fails_when>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright; RC=$?; docker compose -f test/docker-compose.test.yml down --remove-orphans >/dev/null 2>&1; exit $RC</automated>
    <fails_when>exit code is non-zero; the runner output contains `failed` on the summary line or a line beginning with `✘`; a timeout message naming `getByTestId` means a selector was invented rather than verified</fails_when>
    <automated>! grep -rn 'waitForTimeout' test/e2e/</automated>
    <fails_when>exit code 1 — a fixed sleep was used in place of a retrying assertion, which passes on this machine and flakes on a slower one</fails_when>
  </verify>

  <acceptance_criteria>
    - `test/e2e/02-watchlist.spec.ts` and `test/e2e/03-trading.spec.ts` both exist and are collected by `playwright test --list`.
    - `02-watchlist.spec.ts` references `watchlist-add-input`, `watchlist-add-submit`, `watchlist-add-error`, `row-PYPL`, and `remove-PYPL`.
    - `03-trading.spec.ts` references `trade-bar-ticker`, `trade-bar-quantity`, `trade-bar-buy`, `trade-bar-sell`, `trade-bar-confirmation`, `trade-bar-error`, `header-cash`, and `position-row-MSFT`.
    - `03-trading.spec.ts` buys `MSFT` itself before asserting on the position — it contains no assertion that would only pass if another spec ran first.
    - `grep -rn 'waitForTimeout' test/e2e/` returns nothing.
    - The full suite command exits 0 with three spec files present.
  </acceptance_criteria>

  <done>The suite proves, against the packaged container, that a user can add and remove watchlist tickers, buy, sell, and be refused a trade they cannot afford — with cash and positions moving exactly as the product promises.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio visuals and the mocked AI copilot</name>

  <precondition>Task 1's specs are green, so the shared container's behaviour under sequential specs is already observed.</precondition>

  <files>test/e2e/04-portfolio-viz.spec.ts, test/e2e/05-chat.spec.ts</files>

  <read_first>
    - `frontend/components/Heatmap.tsx` line 77 — the `heatmap-tile-{name}` testid
    - `frontend/components/PositionsTable.tsx` lines 66-111 — `position-row-{ticker}` and the empty/loading states
    - `frontend/components/PnLChart.tsx` — note it exposes **no** `data-testid`; the recharts DOM is the only hook, and the Phase 03 decision recorded in `.planning/STATE.md` states its dots render at `r={2}` and are assertable via the `.recharts-dot` class
    - `frontend/components/chat/ChatPanel.tsx` lines 108-333 — `chat-panel`, `chat-toggle`, `chat-messages`, `chat-input`, `chat-send`, `chat-thinking`, `chat-error`, `chat-history-loading`, `chat-history-error`, `chat-action-pills`, and `chat-action-pill` carrying `data-status` with values `executed` or `failed`
    - `backend/app/llm/mock.py` — the whole file; specifically `_TRADE_RE` matching `\b(buy|sell)\b\s+(\d+(?:\.\d+)?)\s+([a-z0-9]{1,5})\b`, `_WATCHLIST_RE` matching `\b(add|remove)\b\s+([a-z0-9]{1,5})\b`, the `_PORTFOLIO_KEYWORDS` tuple (`portfolio`, `cash`, `position`, `holding`), and the grounded reply format `Cash balance is {figure}, across {n} position line(s).`
    - `backend/app/api/portfolio.py` — `build_portfolio`'s emitted keys, to understand what the positions table renders
  </read_first>

  <action>
Author `test/e2e/04-portfolio-viz.spec.ts`. It must create its own position rather than depend on
Task 1's `MSFT` buy: use the trade bar to buy a small quantity of `NVDA`, then assert
`heatmap-tile-NVDA` is visible, `position-row-NVDA` is visible in the positions table and renders
values for quantity, average cost, current price and unrealized P&L, and that the P&L chart has at
least one plotted point — locate it via the recharts `.recharts-dot` class, asserting a count of at
least one rather than an exact number, because snapshot cadence makes an exact count timing
dependent. The backend writes a `portfolio_snapshots` row immediately after every executed trade, so
a point is guaranteed to exist shortly after the buy; use a retrying assertion with an explicit
generous timeout rather than a sleep.

Author `test/e2e/05-chat.spec.ts` against the deterministic mock only — the container runs with
`LLM_MOCK` set to the string `true`, so every reply is reproducible. Phrase each message to hit the
intended mock branch exactly:
  - Grounding: send a message containing one of the portfolio keywords (for example
    `what is my portfolio worth`). Assert `chat-thinking` appears while the request is in flight and
    that the reply rendered in `chat-messages` begins with `Cash balance is` — this is the mock's
    literal grounded-reply format, and it proves the reply is built from real portfolio context.
  - Executed trade: send `buy 1 aapl`, which matches the mock's trade regex. Assert a
    `chat-action-pill` appears carrying `data-status="executed"`, and that `header-cash` decreases.
  - Watchlist action: send `add sofi`, which matches the mock's watchlist regex. Assert an
    `executed` pill appears and `row-SOFI` becomes visible in the watchlist.
  - Failure reported inline: send `sell 9999 aapl`. Assert a `chat-action-pill` appears carrying
    `data-status="failed"` and that `header-cash` is unchanged from the value captured just before.

When selecting an action pill, scope through `chat-action-pill` — never through a bare
`[data-status]` selector. `data-status` is also carried by the connection dot, and Playwright's
strict mode throws when a locator resolves more than one element. Expand the chat panel via
`chat-toggle` first if `chat-input` is not immediately visible.
  </action>

  <verify>
    <automated>npm --prefix test ci >/dev/null && npm --prefix test exec -- playwright test --list 2>&1 | grep -E '04-portfolio-viz|05-chat'</automated>
    <fails_when>`playwright test --list` prints neither spec name, or exits non-zero on a TypeScript parse error</fails_when>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright; RC=$?; docker compose -f test/docker-compose.test.yml down --remove-orphans >/dev/null 2>&1; exit $RC</automated>
    <fails_when>exit code is non-zero; a `strict mode violation` message means a `data-status` selector was left unscoped and matched both a chat pill and the connection dot; a reply assertion failing on `Cash balance is` means the message text did not match the mock's portfolio-keyword branch</fails_when>
    <automated>grep -q 'chat-action-pill' test/e2e/05-chat.spec.ts && grep -q 'executed' test/e2e/05-chat.spec.ts && grep -q 'failed' test/e2e/05-chat.spec.ts && grep -q 'recharts-dot' test/e2e/04-portfolio-viz.spec.ts</automated>
    <fails_when>exit code 1 — the chat spec does not cover both the executed and failed pill states, or the portfolio spec does not assert on a plotted P&L point at all</fails_when>
  </verify>

  <acceptance_criteria>
    - `04-portfolio-viz.spec.ts` buys `NVDA` itself, then references `heatmap-tile-NVDA`, `position-row-NVDA`, and `.recharts-dot`.
    - `05-chat.spec.ts` covers four mock branches: the grounded `Cash balance is` reply, an `executed` trade pill, an `executed` watchlist pill with `row-SOFI` appearing, and a `failed` pill leaving `header-cash` unchanged.
    - Every `data-status` assertion in `05-chat.spec.ts` is scoped through `chat-action-pill`; no bare `[data-status=...]` selector appears in any spec.
    - `chat-thinking` is asserted at least once, covering the loading indicator PLAN.md section 10 requires.
    - The full suite command exits 0 with five spec files present.
  </acceptance_criteria>

  <done>The suite proves the portfolio heatmap, positions table, and P&L chart render real data, and that the AI copilot answers from the real portfolio, executes trades and watchlist changes inline, and reports a failure conversationally rather than crashing.</done>
</task>

<task type="auto">
  <name>Task 3: SSE reconnect, and the full suite as the phase gate</name>

  <precondition>Tasks 1 and 2 are green, so a failure in this task is attributable to the reconnect spec rather than to earlier coverage.</precondition>

  <files>test/e2e/06-sse-reconnect.spec.ts</files>

  <read_first>
    - `frontend/components/ConnectionDot.tsx` lines 41-53 — the component renders a `<span>` with `data-status`, `aria-label`, and `role="status"`; **there is no `data-testid` on the dot**, and its colour classes are arbitrary-value Tailwind forms (`bg-[var(--color-up)]`, `bg-[var(--color-accent-yellow)] animate-pulse`, `bg-[var(--color-down)]`) that contain no colour words
    - `frontend/__tests__/Header.test.tsx` line 43 — the in-repo precedent for this exact idiom: `expect(screen.getByRole("status")).toHaveAttribute("data-status", "disconnected")`
    - `frontend/components/chat/ChatPanel.tsx` lines 113 and 125 — the chat pills also carry `data-status`, with values `executed`/`failed` and **no** `role`, which is why `role="status"` is the scoping attribute
    - `.planning/phases/05-one-command-delivery/05-PATTERNS.md` — Critical Correction #1 in full; RESEARCH.md's own `connection-status` / `toHaveClass(/green/)` example is wrong and must not be copied
    - `.planning/STATE.md` — the Phase 01 decision that the amber `connecting` and `reconnecting` states deliberately share one pulse class, so only the attribute distinguishes them
  </read_first>

  <action>
Author `test/e2e/06-sse-reconnect.spec.ts`. Navigate to `/` and locate the connection dot as
`page.getByRole('status')` — the dot is the only element in the app carrying that role, which is
what makes the locator unambiguous under Playwright strict mode. Assert it has `data-status` equal
to `connected`.

Then call `page.context().setOffline(true)` and assert with a retrying expectation that `data-status`
becomes one of the non-connected values — match against `/connecting|reconnecting|disconnected/`
rather than a single value, because `EventSource`'s native retry means the observed state depends on
where in the retry cycle the assertion lands.

Then call `page.context().setOffline(false)` and assert `data-status` returns to `connected`, with a
generous explicit timeout (roughly ten seconds) to accommodate `EventSource`'s backoff.

Never assert on a class string. The dot's classes are arbitrary-value Tailwind forms referencing CSS
custom properties and contain no colour words at all, so any `toHaveClass(/green/)`-style assertion
can never match. Use `toHaveAttribute` throughout, mirroring the in-repo `Header.test.tsx` idiom.

Place this spec last by filename prefix: `setOffline` mutates the whole browser context, so running
it after every other scenario keeps its effects from bleeding into them.

Finally, run the complete suite one last time and record the observed result. All six scenarios from
PLAN.md section 12 must be present and green: fresh start with streaming prices and $10k, watchlist
add/remove, buy, sell, portfolio visualisation, mocked chat with inline trade execution, and SSE
resilience. Record the spec count and the exit code in the plan summary.
  </action>

  <verify>
    <automated>grep -q "getByRole('status')" test/e2e/06-sse-reconnect.spec.ts || grep -q 'getByRole("status")' test/e2e/06-sse-reconnect.spec.ts</automated>
    <fails_when>exit code 1 — the spec targets the dot some other way; a `getByTestId('connection-status')` selector (the incorrect form in RESEARCH.md's example) does not exist in the component and would time out</fails_when>
    <automated>! grep -nE 'toHaveClass\(/(green|red|amber|yellow)' test/e2e/</automated>
    <fails_when>exit code 1 — a colour-class assertion was written; the dot's classes are arbitrary-value forms like `bg-[var(--color-up)]` and contain no colour words, so such an assertion can never match and the spec would fail for a misleading reason</fails_when>
    <automated>test "$(ls test/e2e/*.spec.ts | wc -l)" -eq 6 && npm --prefix test ci >/dev/null && npm --prefix test exec -- playwright test --list 2>&1 | tail -3</automated>
    <fails_when>the spec-file count is anything other than `6`, or `playwright test --list` reports fewer collected files than exist on disk</fails_when>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright; RC=$?; docker compose -f test/docker-compose.test.yml down --remove-orphans >/dev/null 2>&1; exit $RC</automated>
    <fails_when>exit code is non-zero, or the runner summary reports any count other than all-passed; a reconnect assertion timing out at `connected` after `setOffline(false)` indicates the browser context did not restore, not an application bug — investigate before changing the spec</fails_when>
  </verify>

  <acceptance_criteria>
    - `test/e2e/06-sse-reconnect.spec.ts` selects the dot via `getByRole('status')` and asserts on `data-status` with `toHaveAttribute`.
    - No spec in `test/e2e/` contains a `toHaveClass` assertion matching a colour word.
    - `ls test/e2e/*.spec.ts | wc -l` equals `6`, and the filenames carry the prefixes `01-` through `06-`.
    - The full suite command exits 0, and the observed spec count and exit code are recorded in the plan summary.
    - All six PLAN.md section 12 scenarios are represented: fresh start, watchlist add/remove, buy, sell, portfolio visualisation, mocked chat with inline trade execution, and SSE reconnect.
  </acceptance_criteria>

  <done>One command runs a complete E2E suite against the packaged container covering every scenario PLAN.md section 12 names, and it passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec source → version control | Anything a spec hardcodes — a key, a token, a real account identifier — is permanently in git history |
| Playwright runner → app container | The runner drives the app's full unauthenticated API surface, including the chat endpoint that can reach an external LLM provider if mock mode is not in effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | `test/e2e/05-chat.spec.ts` and sibling specs | medium | mitigate | No spec carries a credential of any kind. The chat spec exercises only the deterministic `LLM_MOCK` branch, and the single placeholder key lives in `test/docker-compose.test.yml` (inherited control from plan 05-04), not in any spec. |
| T-05-14 | Tampering | chat-driven trade execution under test | low | mitigate | AI-initiated trades in the chat spec go through the same Phase 3 validation path as manual trades — there is no second, looser execution route — so the `failed` pill assertion genuinely exercises the rejection path rather than a test-only shortcut. The container's database is disposable per run, so no real portfolio state is affected. |
| T-05-04 | Information Disclosure | E2E `app` service network exposure | low | mitigate | Inherited from plan 05-04: the app service uses `expose:` and is reachable only as `app:8000` on the compose network, so a suite run never publishes a seeded database or the chat endpoint to the host. |
</threat_model>

<verification>
- All six spec files exist under `test/e2e/` with the `01-` through `06-` prefixes and are collected by `playwright test --list`.
- No spec uses `page.waitForTimeout`, a colour-class assertion, or a bare `[data-status]` selector.
- `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0.
- Every scenario named in PLAN.md section 12 maps to at least one spec.
</verification>

<success_criteria>
The Playwright E2E suite specified by PLAN.md section 12 exists, runs against the packaged container
under `LLM_MOCK`, covers fresh start, watchlist add/remove, buy, sell, portfolio visuals, mocked
chat with inline trade execution, and SSE reconnection — and passes. OPS-01 has automated,
container-level coverage end to end.
</success_criteria>

## Artifacts this phase produces

Newly created by **this plan** (excluded from drift verification):

| Artifact | Kind |
|----------|------|
| `test/e2e/02-watchlist.spec.ts` | new file |
| `test/e2e/03-trading.spec.ts` | new file |
| `test/e2e/04-portfolio-viz.spec.ts` | new file |
| `test/e2e/05-chat.spec.ts` | new file |
| `test/e2e/06-sse-reconnect.spec.ts` | new file |

Created by sibling plans in phase 05 (also newly-created, also excluded): `Dockerfile`,
`.dockerignore`, `.env.example`, `docker-compose.yml`, `scripts/start_mac.sh`,
`scripts/stop_mac.sh`, `scripts/start_windows.ps1`, `scripts/stop_windows.ps1`,
`test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`,
`test/docker-compose.test.yml`, `test/e2e/01-fresh-start.spec.ts`, and the `demo` extra in
`backend/pyproject.toml`.

Every `data-testid`, `data-status` value, and `role` this plan's specs assert on is **pre-existing**
frontend surface from Phases 1-4 — this plan introduces no new selectors, no new functions, and no
new environment-variable names.

<output>
Create `.planning/phases/05-one-command-delivery/05-05-SUMMARY.md` when done.
</output>
