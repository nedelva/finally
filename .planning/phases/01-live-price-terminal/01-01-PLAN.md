---
phase: 01-live-price-terminal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - frontend/next-env.d.ts
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/next.config.js
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
  - frontend/vitest.config.ts
  - frontend/vitest.setup.ts
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/app/globals.css
  - frontend/__tests__/usePriceFlash.test.ts
autonomous: true
requirements: [MKT-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No file under frontend/lib/ is excluded from git — the existing six hand-authored modules are tracked, and new files added there in later plans can be committed"
    - "frontend/node_modules/ and frontend/out/ are excluded from git, so the first frontend commit cannot drag in the dependency tree or build output"
    - "npm --prefix frontend run build produces a static export at frontend/out/index.html"
    - "A price increase yields the flash-up class and a price decrease yields the flash-down class, and the class clears itself roughly half a second later"
    - "An unchanged price yields no flash class"
  artifacts:
    - path: ".gitignore"
      provides: "lib/ narrowed to the repo-root Python build dir, plus a Node/Next.js exclusion block"
      contains: "node_modules"
    - path: "frontend/package.json"
      provides: "Frontend dependency manifest pinned to the versions already resolved in node_modules, plus build/test/typecheck scripts"
      contains: "\"next\": \"16.3.5\""
    - path: "frontend/next.config.js"
      provides: "Static export configuration for single-origin serving by FastAPI"
      contains: "output"
    - path: "frontend/tsconfig.json"
      provides: "TypeScript strict configuration with the @/* path alias"
      contains: "strict"
    - path: "frontend/postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin wiring"
      contains: "@tailwindcss/postcss"
    - path: "frontend/app/globals.css"
      provides: "Tailwind v4 import, dark-theme colour tokens, and the price-flash keyframes"
      contains: "flash-up"
    - path: "frontend/app/layout.tsx"
      provides: "Root server-component layout importing globals.css"
      min_lines: 12
    - path: "frontend/app/page.tsx"
      provides: "Minimal terminal shell that makes the static export buildable"
      min_lines: 8
    - path: "frontend/vitest.config.ts"
      provides: "Vitest jsdom configuration with the React plugin and setup file"
      contains: "jsdom"
    - path: "frontend/vitest.setup.ts"
      provides: "jest-dom matcher registration for Vitest"
      contains: "jest-dom"
    - path: "frontend/__tests__/usePriceFlash.test.ts"
      provides: "MKT-02 unit coverage for the pre-existing usePriceFlash hook"
      min_lines: 40
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/app/globals.css"
      via: "side-effect stylesheet import in the root layout"
      pattern: "globals\\.css"
    - from: "frontend/__tests__/usePriceFlash.test.ts"
      to: "frontend/lib/usePriceFlash.ts"
      via: "imports the hook under test from the now-trackable lib directory"
      pattern: "usePriceFlash"
    - from: "frontend/postcss.config.mjs"
      to: "frontend/app/globals.css"
      via: "PostCSS processes the Tailwind import during next build"
      pattern: "tailwindcss"
---

<objective>
Repair the repository exclusion rules that currently make `frontend/lib/` uncommittable, then author the entire Next.js project scaffold — which does not exist on disk — so that the frontend can be built, type-checked, and unit-tested for the first time in this project's life.

Purpose: Two hard blockers sit in front of every other Phase 1 frontend task. First, `.gitignore` line 17 carries a bare `lib/` rule inherited from a Python packaging template; it silently excludes the six hand-authored modules under `frontend/lib/` (verified this session: `git check-ignore -v frontend/lib/hooks.ts` reports `.gitignore:17:lib/`). Any later work there would build and test locally, then vanish from `git status`. Second, `frontend/` has no `package.json`, `next.config.js`, or `tsconfig.json` at all — they were deleted in the "Basic starting point" commit — so nothing can be built or tested until they are authored to match the dependency tree already resolved in `frontend/node_modules`.

Output: A corrected `.gitignore`, six frontend configuration files, a minimal buildable app shell, a working Vitest harness, and the first passing frontend test.
</objective>

<execution_context>
@/Users/valeriu/AICourses/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/valeriu/AICourses/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-price-terminal/01-RESEARCH.md
@.planning/phases/01-live-price-terminal/01-PATTERNS.md
@.planning/phases/01-live-price-terminal/01-VALIDATION.md
@planning/PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair .gitignore so frontend source is trackable and Node artifacts are not [BLOCKING — must complete before any other task in this phase]</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (the whole file — it is a 207-line Python-only template with zero Node entries)
    - `.planning/phases/01-live-price-terminal/01-PATTERNS.md` section "Blocking Repo Issue"
  </read_first>
  <action>
Two edits to `.gitignore`, then stage the six previously-excluded modules.

Edit A — narrow the over-broad rule. Line 17 currently holds a bare directory pattern that git applies at every depth, which is why it swallows `frontend/lib/`. Anchor it to the repository root by prefixing a leading slash, so it keeps covering the Python `setup.py`-era build directory at the top level and stops matching nested directories. Leave the neighbouring `lib64/` entry untouched — nothing in this repo is named that. Do not instead add a negation line; anchoring is the narrower, single-character change and leaves no second rule to reason about.

Edit B — append a new trailing section for the Node/Next.js toolchain, which the template never covered. Add a comment header reading `# Node / Next.js` followed by four patterns: `node_modules/`, `/frontend/out/`, `.next/`, and `*.tsbuildinfo`. This is urgent, not cosmetic: `frontend/node_modules/` (a fully resolved dependency tree) and `frontend/out/` (stale build output from a previous template build) are both currently unexcluded, so the first `git add frontend/` in this phase would commit them. Do NOT add `next-env.d.ts` — `frontend/tsconfig.json` lists it under `include` in Task 2, and keeping it tracked lets `tsc --noEmit` run on a fresh clone before any build has regenerated it.

Then stage the six existing modules that were previously unreachable, by explicit path rather than a glob: `frontend/lib/api.ts`, `frontend/lib/format.ts`, `frontend/lib/hooks.ts`, `frontend/lib/positionMath.ts`, `frontend/lib/types.ts`, `frontend/lib/usePriceFlash.ts`. Do not modify their contents in this task — RESEARCH.md mandates reusing them as-is, and Task 3 plus later plans import them unchanged.

Stage `frontend/next-env.d.ts` in the same step. It is already on disk, is matched by no exclusion rule, and Task 2's `tsconfig.json` lists it under `include` — leaving it untracked would mean a file the type-check depends on shows as untracked indefinitely and is missing from a fresh clone until someone runs a build.

Scope boundary: do not touch the `db/` or `.env` rules. The database arrives in Phase 2 and owns its own exclusions.
  </action>
  <acceptance_criteria>
    - `git check-ignore -q frontend/lib/hooks.ts` exits non-zero (the path is no longer excluded)
    - `git check-ignore -q frontend/node_modules` exits zero
    - `git check-ignore -q frontend/out/index.html` exits zero
    - `git ls-files --error-unmatch` succeeds for all six `frontend/lib/*.ts` modules and for `frontend/next-env.d.ts`
    - `git check-ignore -q backend/.venv/pyvenv.cfg` still exits zero (the pre-existing Python exclusions are intact)
  </acceptance_criteria>
  <verify>
    <automated>! git check-ignore -q frontend/lib/hooks.ts && git check-ignore -q frontend/node_modules && git check-ignore -q frontend/out/index.html && git check-ignore -q backend/.venv/pyvenv.cfg && git ls-files --error-unmatch frontend/lib/api.ts frontend/lib/format.ts frontend/lib/hooks.ts frontend/lib/positionMath.ts frontend/lib/types.ts frontend/lib/usePriceFlash.ts frontend/next-env.d.ts</automated>
    <fails_when>non-zero exit; specifically either `git check-ignore -q frontend/lib/hooks.ts` exiting 0 (path still excluded, so the leading negation fails), or `git ls-files --error-unmatch` printing `error: pathspec ... did not match any file(s) known to git` for one of the seven staged paths</fails_when>
  </verify>
  <done>The six `frontend/lib/*.ts` modules are in the git index, `frontend/node_modules/` and `frontend/out/` are excluded, and the pre-existing Python exclusions still apply.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Next.js project scaffold and prove it builds a static export</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/next.config.js, frontend/tsconfig.json, frontend/postcss.config.mjs, frontend/app/layout.tsx, frontend/app/page.tsx, frontend/app/globals.css</files>
  <read_first>
    - `.planning/phases/01-live-price-terminal/01-RESEARCH.md` section "Code Examples → Frontend config files" (the version-verified source of truth for every file in this task)
    - `frontend/lib/types.ts`, `frontend/lib/format.ts` (the existing modules that `tsc` will now compile for the first time)
    - `frontend/next-env.d.ts` (already on disk; referenced by tsconfig `include`)
    - `planning/PLAN.md` sections 3 and 10 (single-origin static export, dark theme, colour scheme)
  </read_first>
  <action>
Author six files, exactly matching the versions already resolved in `frontend/node_modules` — this is a reconstruction of deleted configuration, not a dependency selection exercise.

`frontend/package.json` — name `finally-frontend`, version `0.1.0`, `private: true`. Dependencies pinned as exact strings with no caret or tilde range: `next` 16.3.5, `react` 19.2.8, `react-dom` 19.2.8, `recharts` 3.10.1. Dev dependencies, also exact: `@testing-library/jest-dom` 7.0.1, `@testing-library/react` 16.3.3, `@testing-library/user-event` 14.6.7, `@types/node` 22.20.3, `@types/react` 19.3.0, `@types/react-dom` 19.3.0, `@vitejs/plugin-react` 6.1.1, `eslint` 9.39.5, `eslint-config-next` 16.3.5, `jsdom` 30.1.0, `postcss` 8.5.28, `tailwindcss` 4.3.3, `@tailwindcss/postcss` 4.3.3, `typescript` 5.9.3, `vitest` 5.0.1. Scripts: `dev` runs `next dev`, `build` runs `next build`, `lint` runs `next lint`, `test` runs `vitest run`, `test:watch` runs `vitest`, and `typecheck` runs `tsc --noEmit`. Exact pins are deliberate — RESEARCH.md warns that letting npm re-resolve could pull TypeScript 7, a recent major.

`frontend/next.config.js` — CommonJS module exporting a config object with `output` set to the string `export` and `trailingSlash` set to true. The trailing slash pairs with the `html=True` directory-to-index resolution that the backend's static mount uses in plan 01-02.

`frontend/tsconfig.json` — use RESEARCH.md's proposed compiler options verbatim: target es2020; lib dom, dom.iterable, esnext; `allowJs` false; `skipLibCheck` true; `strict` true; `noEmit` true; `esModuleInterop` true; module esnext; `moduleResolution` bundler; `resolveJsonModule` true; `isolatedModules` true; jsx preserve; `incremental` true; the `next` plugin entry; `baseUrl` "."; and a `paths` mapping of `@/*` to `./*`. Include `next-env.d.ts`, all `.ts` and `.tsx` files, and `.next/types/**/*.ts`; exclude `node_modules`.

`frontend/postcss.config.mjs` — default-export an object whose `plugins` key maps `@tailwindcss/postcss` to an empty object. Tailwind v4 is CSS-config-first; do not author a `tailwind.config.js`, which v4.3.3 ignores unless explicitly wired.

`frontend/app/globals.css` — start with the Tailwind v4 single-line import. Then a `@theme` block defining the project colour tokens from PLAN.md section 2: `--color-bg` `#0d1117`, `--color-panel` `#161b22`, `--color-border` `#30363d`, `--color-accent-yellow` `#ecad0a`, `--color-primary-blue` `#209dd7`, `--color-secondary-purple` `#753991`, `--color-up` `#16a34a`, `--color-down` `#dc2626`. Then the price-flash effect that MKT-02 depends on: two classes named `flash-up` and `flash-down`, each applying a named keyframe animation over 550ms with an ease-out curve, and two matching `@keyframes` rules that start at a translucent tint of the up/down colour and end at `transparent`. Use a keyframe animation rather than a bare CSS transition — the hook in `frontend/lib/usePriceFlash.ts` holds the class for 550ms and then removes it, and a transition would snap back instantly on removal instead of fading. Do not build a second animation mechanism anywhere else; this class pair is the single contract, and `frontend/lib/usePriceFlash.ts` line 24 already emits exactly these two names.

`frontend/app/layout.tsx` — a root layout server component. No `"use client"` directive (this is the one frontend file that must stay a server component). Side-effect import of `./globals.css`. Export a `metadata` object with title `FinAlly — AI Trading Workstation`. Default-export `RootLayout`, taking `children` typed as `React.ReactNode`, rendering `html` with `lang="en"` and a `body` carrying the dark background and light foreground utility classes built from the theme tokens.

`frontend/app/page.tsx` — a minimal shell whose only job right now is to make the export buildable and to put a real string on the page. No `"use client"` yet; plan 01-02 converts this file into the live-streaming client component. Render a header element containing the text `FinAlly` and a short subtitle. Include the literal text `Simulated market data` in the subtitle — plan 01-02 relies on a known string being present in the served HTML, and PLAN.md's simulator-by-default design means a Bloomberg-styled terminal must never imply the prices are real.

Then install and verify, in this order. Run `npm --prefix frontend install` to regenerate `package-lock.json` against the tree already on disk. Immediately assert that nothing drifted: read back the installed `next` version and confirm it is still `16.3.5`; if npm re-resolved it, the pins are wrong and must be corrected before proceeding rather than accepted.

Finally, expect and fix first-compile fallout. `frontend/lib/*.ts` has never been compiled under any tsconfig — the configs were deleted after those modules were written — and the `include` pattern pulls all of `api.ts`, `hooks.ts`, `positionMath.ts`, `format.ts`, and `types.ts` into the program even though no page imports them yet. Resolving whatever type errors that first `tsc` run surfaces in those five modules is in scope for this task. Fix them properly, at the site of the error. Do not narrow the `include` pattern to exclude `lib`, and do not set `typescript.ignoreBuildErrors` in the Next config — both hide the failure instead of resolving it, and later plans import these modules directly.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits zero with no diagnostics
    - The installed `next` package version reads exactly `16.3.5` after `npm install`
    - `npm --prefix frontend run build` exits zero
    - `frontend/out/index.html` exists after the build and contains the string `FinAlly`
    - `frontend/next.config.js` sets `output` to `export` (not `standalone`, not absent)
    - `frontend/app/globals.css` defines both a `flash-up` and a `flash-down` class, and the names match what `frontend/lib/usePriceFlash.ts` returns
    - `frontend/app/layout.tsx` is a server component: it exports a `metadata` object, which Next.js only permits from a server component, so the successful build is itself the proof
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in stdout</fails_when>
    <automated>node -p "require('/Users/valeriu/AICourses/finally/frontend/node_modules/next/package.json').version"</automated>
    <fails_when>prints anything other than `16.3.5` — npm re-resolved a different major/minor and the exact pins in package.json are wrong</fails_when>
    <automated>npm --prefix frontend run build && grep -q FinAlly frontend/out/index.html</automated>
    <fails_when>non-zero exit; specifically `Failed to compile` in the build output, or grep exiting 1 because `frontend/out/index.html` is absent or is the stale pre-existing template page rather than the newly built shell</fails_when>
  </verify>
  <done>`frontend/` is a buildable, type-clean Next.js static-export project whose dependency versions match the pre-resolved tree, and `frontend/out/index.html` is the freshly built FinAlly shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stand up the Vitest harness and cover the price-flash hook (MKT-02)</name>
  <files>frontend/vitest.config.ts, frontend/vitest.setup.ts, frontend/__tests__/usePriceFlash.test.ts</files>
  <read_first>
    - `frontend/lib/usePriceFlash.ts` (the full 35-line hook under test — it is the subject, and must not be modified)
    - `.planning/phases/01-live-price-terminal/01-VALIDATION.md` sections "Test Infrastructure → Frontend" and "Wave 0 Requirements"
    - `frontend/package.json` and `frontend/tsconfig.json` as written in Task 2 (the `@/*` alias must be mirrored in the Vitest config)
  </read_first>
  <behavior>
    - With a stable price, the hook returns an empty string and applies no class
    - First render returns an empty string even though a price is supplied (there is no prior value to compare against)
    - A price change from 190.00 to 191.00 returns `flash-up`
    - A price change from 190.00 to 189.00 returns `flash-down`
    - After advancing timers by 550ms following a change, the returned class is an empty string again
    - Re-rendering with the identical price returns an empty string (no flash for an unchanged value)
    - A transition out of `null` or `undefined` into a real price returns an empty string, not a flash
  </behavior>
  <action>
Author the test harness that `frontend/node_modules` has been ready for all along but that has no configuration file.

`frontend/vitest.config.ts` — import `defineConfig` from vitest's config entry and the React plugin from `@vitejs/plugin-react`. Register the React plugin. Set `test.environment` to `jsdom`, `test.globals` to true, and `test.setupFiles` to the relative path of `./vitest.setup.ts`. Add a `resolve.alias` entry mapping `@` to the frontend directory root so the alias behaves identically to the `paths` mapping authored in `tsconfig.json` — a test importing through `@/lib/...` must resolve the same way a component does.

`frontend/vitest.setup.ts` — a single side-effect import of the Vitest entry point of `@testing-library/jest-dom`, which registers the DOM matchers.

`frontend/__tests__/usePriceFlash.test.ts` — cover the hook already sitting in `frontend/lib/usePriceFlash.ts`, which has zero coverage today. Use `renderHook` and `act` from `@testing-library/react`. Because the hook clears its class via `setTimeout` at 550ms, install Vitest's fake timers in a `beforeEach` and restore real timers in an `afterEach`; advance time inside `act` so the resulting state update is flushed. Write one assertion per behaviour listed in the `<behavior>` block above, driving price changes through `renderHook`'s `rerender` with new props. Assert on the exact returned strings `flash-up`, `flash-down`, and the empty string — these are the literal class names `frontend/app/globals.css` defines, so the test is what binds the hook's output to the stylesheet contract.

Do not modify `frontend/lib/usePriceFlash.ts`. If a test appears to fail because of hook behaviour rather than test setup, the correct response is to re-read the hook and fix the test's expectation — the hook is pre-existing, reviewed source that RESEARCH.md mandates reusing verbatim.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- usePriceFlash` exits zero and reports at least 6 passing tests
    - The test file asserts on the literal strings `flash-up` and `flash-down`
    - `frontend/lib/usePriceFlash.ts` is byte-identical to its state before this task
    - `frontend/vitest.config.ts` sets the test environment to `jsdom`
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend run test -- usePriceFlash</automated>
    <fails_when>non-zero exit; `No test files found` in the output; or a summary line reporting `Tests  0 passed` (a filter that matched nothing exits 0 on some Vitest configurations, so an absent or zero test count is the failure signal, not just the exit code)</fails_when>
    <automated>git diff --quiet -- frontend/lib/usePriceFlash.ts</automated>
    <fails_when>non-zero exit, meaning the hook under test was modified instead of the test being corrected</fails_when>
  </verify>
  <done>Vitest runs against jsdom with jest-dom matchers registered, and the price-flash hook that MKT-02 depends on has passing behavioural coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `frontend/node_modules` | Third-party package code enters the build and, via `next build`, the shipped bundle |
| developer workstation → git history | Files staged here become permanent repository content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm --prefix frontend install` (Task 2) | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` covers every package in `frontend/package.json`; all verdicts are OK / Approved with no `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries, and all are already resolved on disk — so no blocking legitimacy checkpoint is warranted. The residual risk is silent version drift during lockfile regeneration, mitigated by exact (range-free) pins plus the post-install assertion that `next` still reads `16.3.5`. |
| T-01-02 | Information Disclosure | `git add` of `frontend/` after the `.gitignore` repair (Task 1) | medium | mitigate | Narrowing `lib/` widens what git can see, so the same task adds the Node exclusion block before anything is staged. Only six named `frontend/lib/*.ts` paths are staged explicitly — never a directory glob — so `node_modules/` and `out/` cannot ride along even if the new rules were wrong. |
| T-01-03 | Tampering | `frontend/app/globals.css` theme tokens and flash classes | low | accept | Static stylesheet with no user-controlled input; no interpolation surface exists this phase. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and paths created by this plan (newly created — excluded from drift verification):

- Files: `frontend/package.json`, `frontend/package-lock.json`, `frontend/next.config.js`, `frontend/tsconfig.json`, `frontend/postcss.config.mjs`, `frontend/vitest.config.ts`, `frontend/vitest.setup.ts`, `frontend/app/layout.tsx`, `frontend/app/page.tsx`, `frontend/app/globals.css`, `frontend/__tests__/usePriceFlash.test.ts`
- React exports: `RootLayout` (default export of `frontend/app/layout.tsx`), `metadata` (named export of `frontend/app/layout.tsx`), `Page` (default export of `frontend/app/page.tsx`)
- CSS class names: `flash-up`, `flash-down`
- CSS custom properties: `--color-bg`, `--color-panel`, `--color-border`, `--color-accent-yellow`, `--color-primary-blue`, `--color-secondary-purple`, `--color-up`, `--color-down`
- npm scripts: `dev`, `build`, `lint`, `test`, `test:watch`, `typecheck`
- `.gitignore` rules: `/lib/` (narrowed), `node_modules/`, `/frontend/out/`, `.next/`, `*.tsbuildinfo`
- Build output directory (gitignored, not source): `frontend/out/`
</artifacts_this_phase_produces>

<verification>
- `git check-ignore -q frontend/lib/hooks.ts` exits non-zero; `git check-ignore -q frontend/node_modules` exits zero
- `npm --prefix frontend run typecheck` is clean
- `npm --prefix frontend run build` succeeds and `frontend/out/index.html` contains `FinAlly`
- `npm --prefix frontend run test` is green
- `uv run --directory backend --extra dev pytest -q` still reports 73 passed (this plan must not disturb the backend baseline)
</verification>

<success_criteria>
The frontend is a buildable, type-clean, testable Next.js static-export project; `frontend/lib/` is committable for the first time; and the price-flash behaviour MKT-02 requires has passing automated coverage.
</success_criteria>

<output>
Create `.planning/phases/01-live-price-terminal/01-01-SUMMARY.md` when done
</output>
